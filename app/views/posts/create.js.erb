<% if @post.new_record? %>
	alert("Failed to upload image: <%= j @post.errors.full_messages.join(', ').html_safe %>");
<% else %>
	console.log("<%= j render @post %>")
	new_post = $("<%= j render @post %>")
	$(".post_form").after( new_post)
	$('#post_content').val('')
<% end %>