<% if @post.new_record? %>
	alert("Failed to upload image: <%= j @post.errors.full_messages.join(', ').html_safe %>");
<% else %>
	$("#images").prepend("<%= j render @post %>")
	$('#post_content').val('')
<% end %>