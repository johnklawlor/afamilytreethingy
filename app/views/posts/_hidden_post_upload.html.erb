<%= form_for @member.posts.new, html: { id: 'hidden_new_post', class: 'file-upload hide' } do |f| %>
	<%= f.file_field :tmp_image, multiple: true, name: "post[tmp_image]" %>
	<%= f.hidden_field :member_id, value: @member.id %>
	<%= f.hidden_field :from_member, value: current_member.id %>
<% end %>