<%= form_for @member.images.new, html: { id: 'hidden_new_image' } do |f| %>
	<%= f.file_field :image, multiple: true, name: "image[image]", id: "hidden_image_upload_field" %>
	<%= f.hidden_field :member_id, value: current_member.id %>
<% end %>