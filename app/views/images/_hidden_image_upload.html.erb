<%= form_for @member.images.new, html: { id: 'hidden_new_image', class: 'file-upload hide' } do |f| %>
	<%= f.file_field :image, multiple: true, name: "image[image]" %>
	<%= f.hidden_field :member_id, value: current_member.id %>
<% end %>