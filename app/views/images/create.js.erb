<% if @images.new_record? %>
	alert("Failed to upload image: <%= j @images.errors.full_messages.join(', ').html_safe %>");
<% else %>
	$("#image_upload_section").after("<%= j( render( @images) )%>");
<% end %>