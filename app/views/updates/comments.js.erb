<% unless @new_comments.nil? %>
	<% @new_comments.each do |comment| %>
		$('#image_comments').prepend( "<%= j render partial: 'comments/comment', object: comment, as: :comment %>" )
	<% end %>
<% end %>
console.log( ' <%= j @new_comment.inspect %>')